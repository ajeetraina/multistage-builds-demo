{
  "name": "simple-app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp app.js dist/index.js",
    "test": "echo 'Tests passed'"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
